# Change prefix
set -g prefix C-t

set -g default-terminal xterm-256color

# Sane scrolling
set -g terminal-overrides 'xterm*:smcup@:rmcup@'

# Resize
bind-key 'i' select-pane
bind-key 'k' select-pane
bind-key 'j' select-pane
bind-key 'l' select-pane
