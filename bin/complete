#!/usr/bin/env bash

set -euo pipefail

cd /mnt/storage/completed
sudo chown -R paho .
cleardirs

for f in $(fd -tf -d1); do
  echo "$f:"
  imdb-rename --mkdir --template "$IMDB_RENAME_TEMPLATE" "$f" || true
done