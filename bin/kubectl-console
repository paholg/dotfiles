#!/bin/bash -e

if [ $# -eq 0 ]; then
    cmd='bundle exec rails c'
else
    cmd=$1
fi

kubectl scale --replicas=1 deployment server-console
kubectl exec -it $(kubectl get pods -lrails_console=true -o jsonpath='{.items[0].metadata.name}') $cmd
